name: Apply customer name metadata patch
on:
  push:
    branches:
      - agent/customer-name-metadata
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/customer-name-metadata
      - name: Apply patch
        run: python scripts/agent-customer-name-patch.py
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: preserve customer name metadata"
          git push origin HEAD:agent/customer-name-metadata
